* support other tumblr blogs than just BM
* fix startup issues
* large slideshow
* slideshow per year ? random slideshow ? latest ?
* properly use GAE infrastructure (entities, memcache, etc...)
* grails page improvements. Tag libs ? 
* nice layout for slideshows
* new features?
** hardcoded link page of the day
** browsing next/previous
** calendar
